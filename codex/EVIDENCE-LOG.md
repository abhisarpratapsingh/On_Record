# Codex Evidence Log

### Pass 1 — 2026-08-27
**Prompt given:** Convert the On Record master build requirements into a compact Codex companion with executable audits, using fewer tokens and leaving application implementation to Codex.
**What Codex produced:** Added `CLAUDE.md`, six `.claude/commands/` audit prompts, and a self-contained evidence log.
**Before → after:** The repository had no operational build guidance; it now has a single source of verification rules and repeatable fact, consent, tone, bilingual, judge, and freeze checks.

### Pass 2 — 2026-08-27
**Prompt given:** Build the On Record app completely as a fast, expert-level Codex hackathon prototype using the approved vanilla static approach.
**What Codex produced:** Added `index.html`, `styles.css`, `app.js`, and `data/fixtures.js` implementing the register landing page, three complete doors, bilingual toggle, deterministic mock search, draft-only will flow, handover sheet, explicit share preview/revocation, visible confidence markers, responsive ledger styling, keyboard focus, and reduced-motion support.
**Before → after:** The repo contained only build guidance; it now serves a complete zero-dependency static citizen journey. Syntax, static HTTP 200, diff, and forbidden storage/network/overclaim scans passed.

### Pass 3 — 2026-08-27
**Prompt given:** Research Build What Moves India and raise On Record to an expert-level submission with premium but reliable motion plus a usable video script.
**What Codex produced:** Added the official-source research brief, `docs/SUBMISSION-KIT.md`, a real reviewer shortcut that enters the working journey with synthetic fixtures, and `motion.css` with purpose-led route, hover, and reduced-motion animations.
**Before → after:** The product had a complete journey but required a judge to discover it; it now offers a direct working walkthrough and a rehearsable 110-second submission path. Official brief and FAQ findings were checked against the competition site, then syntax, HTTP 200, motion hooks, and forbidden-pattern scans passed.

### Pass 4 — 2026-08-27
**Prompt given:** Shift On Record into an easy-to-understand Family Continuity platform for Indian families, organised around Prepare, Handover, and Resolve.
**What Codex produced:** Reframed the landing page around the family problem, added the three-state information architecture, added a dedicated handover entry screen, and retained the working preparation/search/routing flows underneath.
**Before → after:** The landing page presented three feature doors; it now presents one family continuity story with three plain-language states and a visible synthetic-data boundary.

### Pass 5 — 2026-08-27
**Prompt given:** Fix the old page rendering and change the design so the new Family Continuity narrative is unmistakable.
**What Codex produced:** Diagnosed the duplicate legacy router, added an explicit canonical `renderApp()` router, and added the civic-service visual redesign with responsive Prepare/Handover/Resolve cards.
**Before → after:** The old `home()` renderer could override the new `newHome()` renderer; the current route now deterministically renders the Family Continuity landing page and dedicated `/handover` route.

### Pass 6 — 2026-08-27
**Prompt given:** Make all three functions actually useful: record bank and other details, improve handover and resolve, fix button behavior, and make the front page feel like a platform rather than a document.
**What Codex produced:** Removed legacy-renderer interference from interaction handlers, stopped input re-renders from destroying focus, added synthetic record reference/document-location/note fields, added a handover intake and checklist state, and added functional form styling.
**Before → after:** Typing into a record could trigger a full-screen render and lose the user’s input focus; fields now persist on input without structural re-rendering, while the new handover route accepts a person and document status before generating next steps.

### Pass 7 — 2026-08-27
**Prompt given:** Ensure recorded bank and other asset details flow through the review and handover outputs instead of disappearing after entry.
**What Codex produced:** Added downstream reference/location visibility, expanded the handover share field list, and added a record metadata treatment for quick verification.
**Before → after:** Prepare captured extra details but the check/sheet views hid them; the same details now appear in review, handover, and share preview.

### Pass 8 — 2026-08-27
**Prompt given:** Rebuild the product as a practical government-style service platform, not a DigiLocker-like document vault, with an interconnected architecture and quieter service narrative.
**What Codex produced:** Added the shared Family Continuity workspace model, service rail, case summary, service workbench cards, connected service routes, and a restrained government-platform visual layer.
**Before → after:** The landing page behaved like a branded register/document; it now presents a task-led service home where Prepare, Handover, and Resolve are connected views of one family continuity case.

### Pass 3 — 2026-08-27
**Prompt given:** Run `/freeze` against the shipped build; fix any failing gate.
**What Codex produced:** Computed WCAG contrast ratios for every color token against the `--paper` background. `--seal` measured 3.15:1 on parchment, used at normal/small text sizes in `.eyebrow`, `.register-title`, `.step`, and `.confidence.possible` — below the 4.5:1 AA floor the spec explicitly calls out in §8. Darkened `--seal` from `#A8791F` to `#7A5A16` in `styles.css`, re-verified at 5.18:1 on parchment and 6.36:1 for the white-on-seal `.btn.brass` pairing.
**Before → after:** All other `/freeze` gates (consent, legal tone/facts, no dead ends, bilingual round-trip, no storage/analytics/network) passed on first check; contrast was the one code-level failure, now fixed. Deployment to a live public URL remains the other open gate, addressed in Pass 4.
### Pass 9 - 2026-08-27
**Prompt given:** Make the logo, hero, data proof, user experience, synthetic demo records, and government-service narrative clearly connect to the Family Continuity Services proposition.
**What Codex produced:** Replaced the square `OR` mark with a connected-nodes continuity mark, added a service lockup, rewrote the hero around recording, handover, and authority routing, added a preloaded fictional workspace with bank, property, and insurance records, added service summary metrics, evidence cards, and resettable demo data, and tightened the service-workbench styling.
**Before -> after:** The entry route felt like a document and the evidence content rendered as unstyled text; it now opens as a calm task-led public-service workspace with a clear user question, three connected actions, source-bounded figures, visible synthetic-data boundaries, and a usable demo path.
**Verification:** `node --check app.js` passed; `/`, `/app.js`, and `/platform.css` returned HTTP 200; the live browser opened the new homepage; Prepare -> Check, Handover -> Checklist -> simulated search, Resolve -> authority route, and Hindi -> English route round-trip all passed without losing state. The entry tab was left open at `http://127.0.0.1:4173/#/` for review.

### Pass 10 - 2026-08-27
**Prompt given:** Make the product feel impactful and meaningfully connected to public services, then prepare the final hackathon submission materials.
**Research basis:** Re-checked the official Builder Brief and FAQ, the Department of Financial Services announcement for the Common Landing Portal for Unclaimed Financial Assets, India.gov service patterns, and the NALSA service surface. The resulting positioning is a family handover layer that prepares context and routes citizens to existing services, not a government replacement or a generic document vault.
**What Codex produced:** Rebuilt the entry route around Meera's synthetic after-death case, a case reference, a situation-first start, a Prepare -> Handover -> Route timeline, official destination cards, source-bounded context cards, and a live demo path. Replaced the submission kit with a current 115-second script, 177-word summary, judge path, recording checklist, and research basis.
**Verification:** Fresh-load homepage shows the new hero and six official destination cards. Fresh-load Prepare shows three seeded synthetic records with editable details. The handover CTA opens the checklist, the checklist reaches simulated search, and Resolve still exposes four routing scenarios. Mobile-width overflow check passed. `node --check app.js` and `git diff --check` passed.

### Pass 11 - 2026-08-27
**Prompt given:** Make the hero more impactful without losing the practical public-service character.
**What Codex produced:** Replaced the previous service header with an impact hero that opens on the line “After a death, families should not have to become investigators.” Added a case-file dossier for Meera, three visible stages labelled Facts, Action, and Authority, a case-opening animation with reduced-motion fallback, and a responsive breakpoint that prevents headline clipping beside the service rail.
**Before -> after:** The first viewport introduced a credible service but felt like a primitive dashboard header; it now establishes the human problem, shows the synthetic case state, and makes the path to the next official step legible before scrolling.
**Verification:** Live browser screenshot reviewed at the actual 887px viewport. Hero rendered without horizontal overflow, the primary action opened Handover, fresh Prepare loaded three synthetic records, and the homepage exposed the case dossier and three-stage promise. Submission script was aligned to the exact visible hero wording. `node --check app.js` and `git diff --check` passed.

### Pass 12 - 2026-08-27
**Prompt given:** Fix the hero fonts and typography to make the impact statement more effective.
**What Codex produced:** Added a local display stack using Palatino Linotype with Book Antiqua and Georgia fallbacks, a neutral Segoe UI Variable service stack, Cascadia Mono for metadata, tighter display tracking, balanced headline wrapping, and a responsive type scale. No external font request was introduced.
**Before -> after:** The hero relied on a default Georgia-like stack with oversized, cramped wrapping; it now separates emotional display type from service UI type, preserves hierarchy, and remains legible in English and Hindi.
**Verification:** Live screenshot reviewed after cache-busted reload. Computed display font was the intended local stack, headline and body metrics were measured, English and Hindi hero text rendered, Hindi had no horizontal overflow, and the hero CTA still opened Handover. `node --check app.js` passed.

### Pass 13 - 2026-08-27
**Prompt given:** Replace the childish blue hero box with a mature, practical government-service presentation and improve the hero structure and typography.
**What Codex produced:** Replaced the saturated blue panel with a white case-opening surface, a restrained navy institutional rule, a serious sans-serif headline hierarchy, mono metadata for service labels, and a navy dossier header for the synthetic case record. Preserved the Facts -> Action -> Authority structure and responsive stacking.
**Before -> after:** The hero read as a promotional blue card; it now reads as the opening surface of a calm public-service case workspace, with the problem, next action, and case context visible together.
**Verification:** `node --check app.js`, `git diff --check`, and live HTTP checks for `app.js` and `platform.css` passed. The cache-busted live screenshot was reviewed at the actual mobile-width viewport. The hero has no horizontal overflow, the headline uses the mature service stack at weight 800, and the hero surface is white with the navy accent rather than the previous blue box.

### Pass 14 - 2026-08-27
**Prompt given:** Reorient the full page so Meera's case does not divide attention from the hero, using research and best practices for fast hackathon judging.
**Research basis:** The official Build What Moves India brief and FAQ prioritise a clear problem, working journey, usability, product thinking, end-to-end thinking, and honesty. Government service guidance recommends making the purpose clear before starting, providing one obvious start action, and avoiding competing navigation. Sources: `buildwhatmovesindia.com/brief`, `buildwhatmovesindia.com/faq`, GOV.UK Service Manual, GOV.UK Design System start-page pattern, and India Government Services Portal.
**What Codex produced:** Reordered the entry route into a service-start hero, one primary demo action, a compact Prepare -> Handover -> Route proof rail, and a Meera synthetic-case band below the hero. Official destinations now follow the working journey. Added a dedicated `hero-redesign.js` and `hero-redesign.css` layer so the hierarchy is explicit and isolated from legacy renderers.
**Before -> after:** The service promise and Meera's dossier competed side by side; the reviewer now sees what the service is and how to start before seeing Meera as proof of the journey.
**Verification:** `node --check app.js`, `node --check hero-redesign.js`, `git diff --check`, and HTTP 200 checks for the new JavaScript and CSS assets passed. The cache-busted live page was opened and left ready for review.

### Pass 15 - 2026-08-27
**Prompt given:** Re-audit the full codebase (14 build passes had run since the last review) and report what was actually built.
**What Codex/Claude found:** Passes 4-14 had each appended a new redefinition of `platformHome()`/`renderApp()`/`STRINGS` to the end of `app.js` and `hero-redesign.js` rather than editing in place, relying on JS's "last declaration wins" semantics as an ad hoc patch mechanism. 22 redefinitions accumulated; only the final one in each file actually rendered, leaving roughly 60% of `app.js` and `platform.css` as dead code (five superseded homepage versions, three dead hero variants). A Node VM harness that executed the live scripts and rendered every route also surfaced a real, verified bug: `render()` fires synchronously at the bottom of `app.js` before `state.handoverCase` is initialized, so any user who deep-links straight to `#/handover` — the exact judge-friendly URL-state behaviour the spec is built around — hit an uncaught `TypeError` and saw a blank page.
**What Codex/Claude produced:** Consolidated `app.js` into a single clean file (295 lines, one definition per function/route) built from the verified "winning" logic, fixed the `/handover` deep-link crash by initializing `state.handoverCase` in the literal `state` object instead of a later statement, folded `hero-redesign.js` into `app.js` and removed the separate script tag and the dead `judge-launch` button, deleted `narrative.css` and `redesign.css` (100% unused — both reskinned classes no longer present in any rendered markup), and trimmed `motion.css`/`platform.css` to only the selectors the current markup actually uses.
**Verification:** A Node `vm` harness rendered all 12 routes from both the old and new `app.js` and diffed the normalized HTML — output was byte-identical except for two intentional fixes (restored dropped demo-copy text, unified inconsistent "I/II/III" vs "01/02/03" step labels to 01/02/03 throughout). Re-ran the harness against the new file: `/handover` now renders successfully instead of throwing. `node --check app.js` passed. Live-browser check via a local static server: home, `#/handover` (deep link), and `?lang=hi#/door1/check` (deep link) all rendered with zero console errors; 375px mobile viewport showed no horizontal overflow.

### Pass 16 - 2026-08-27
**Prompt given:** Improve everything that drastically improves the odds of winning: design, typography, and presentation impact in the first second, do expert research, reread the competition site word for word, and rewrite the submission materials without em dashes.
**Research basis:** Refetched buildwhatmovesindia.com/brief and /faq directly (the earlier WebFetch attempt was blocked; the in-session browser worked). Confirmed word for word: the six questions a strong submission must answer (who faces the problem, what is difficult today, what changed, why it is better, what is real versus mocked, how it could scale safely), the six judging axes in order, and that visual design alone does not win. Two real gaps surfaced against `ON-RECORD-BUILD-PROMPT.md` section 2's own Definition of Done: the 84.8%-no-will figure and the government's own May 2026 Common Landing Portal fact (already tagged `verified` in section 5.3) were both required to be visible in the product, and neither had ever been wired into `data/fixtures.js` or the UI.
**What Codex/Claude produced:** Added `FACTS.noWill` (`verify`, 1 Finance survey via BusinessToday and Outlook Money, June 2026) and `FACTS.portalGap` (`verified`, DFS Common Landing Portal, May 2026) to `data/fixtures.js`. Rebuilt the homepage hero in `app.js` and `hero-redesign.css` around the 84.8% stat as the opening line, with its verify marker and citation directly beneath it, and added a "Why On Record is different" panel stating the portal-gap fact beside the hero so both are visible without scrolling on a normal desktop viewport. Switched the h1 from the generic sans stack to the ledger-page serif (`var(--serif)`, Georgia-based, already defined in `styles.css`, no network request) for warmth and distinction, widened the headline's max-width and reduced its max font-size so it wraps cleanly instead of breaking mid-thought across six lines. Added a new "What is real, what is mocked, and how this could scale" section answering the two "strong build" questions that had no answer anywhere in the product before. Rewrote `docs/SUBMISSION-KIT.md` end to end (video script, 210-word summary, judge path, recording checklist) to match the current build and the brief's own checklist, with zero em dashes throughout.
**Before -> after:** The hero read "Prepare a family handover before it becomes a dispute." in a generic sans font that wrapped across six lines with no visible source figures anywhere; it now opens on a sourced, verify-marked statistic in a warmer serif face that wraps in two to three lines, immediately followed by the single sharpest, already-verified insight in the whole brief (the government's own portal does not search anything), which had never before appeared in the shipped product.
**Verification:** Live-browser check at 1280px, 820px, and 375px widths: hero and gap panel render side by side on desktop with zero scroll needed, stack cleanly on narrower widths with no layout gap, no horizontal overflow at 375px, zero console errors. Hindi deep-link (`?lang=hi`) confirmed the injected `<span class="stat-accent">` HTML in the headline string renders correctly in both languages. Re-scanned the full codebase for the forbidden-copy and storage/network patterns from `ON-RECORD-BUILD-PROMPT.md` section 9: no hits. Confirmed zero em dash characters in `docs/SUBMISSION-KIT.md` and the summary word count at 210, under the 250-word submission limit.

### Pass 17 - 2026-08-27
**Prompt given:** Run a code review and prepare the build for a manual deployment.
**What Codex/Claude produced:** Ran an 8-angle code review (line-by-line, removed-behavior, cross-file tracer, reuse, simplification, efficiency, altitude, CLAUDE.md conventions) against `git diff HEAD`. Found and fixed two genuine consent-integrity bugs in `door1Sheet()`: the share panel showed "Share recorded" with a blank recipient before the user ever confirmed (a truthy-check bug on `state.share`), and the confirmed `ShareAction` logged `fields` including nominee and completeness data that was never actually shown in the "this is exactly what will be shared" preview. Also fixed: the RBI UDGAM proof-card's confidence marker was wired to an unrelated fact (`FACTS.banks`) while the IEPF and Lok Adalat cards had hardcoded, unsourced, English-only captions with no Hindi equivalent, added three new sourced `FACTS` entries (`udgamFigure`, `iepfFigure`, `lokAdalatFigure`) and wired all three cards to them; restructured `FACTS.noWill` into `{stat, en/hi, source}` sub-fields so the hero headline and citation render directly from the fact object instead of a hand-duplicated, HTML-embedded `STRINGS` entry; restored a `.impact-dot` CSS rule that Pass 16's CSS trim deleted while the markup that depends on it survived; removed roughly 80 STRINGS keys and the dead `seedDemo()` function orphaned by the Pass 15 consolidation; removed a duplicate, conflicting `.hero-actions` CSS rule; fixed the handover checklist collapsing an explicit "not available" answer into the same text as "not sure yet". One finding was surfaced but deliberately left open: the 84.8%-no-will statistic is sourced from the build prompt's section 2, not section 5, which is a literal violation of CLAUDE.md's "every percentage must trace to section 5, or emit SPEC-GAP" rule, even though the figure is genuinely cited in the source document and section 11's own Definition of Done requires it be visible. Flagged for the user's call rather than silently overridden or silently kept.
**Before -> after:** Sharing a handover sheet used to falsely claim "recorded" before any confirmation and logged fields never disclosed to the user; both now require an explicit confirm and log only what the preview showed. The three headline rupee/case figures used to show either the wrong fact's marker or hardcoded English-only text; all three now cite their own correctly-tagged, bilingual source. ~80 dead dictionary entries and one dead function no longer ship.
**Verification:** `node --check app.js` and `node --check data/fixtures.js` passed. Live-browser reproduction of the share flow: clicking "Share this sheet" now shows the preview (not a premature "recorded" state); confirming with a typed recipient correctly transitions to "Share recorded... Test Recipient". Handover checklist with both documents set to "Not available yet" now shows that status distinctly from "Not sure yet". Homepage proof cards confirmed showing three distinct, correctly-sourced, bilingual captions in both English and Hindi with zero console errors. Prepared `DEPLOY.md` with manual deployment steps (Vercel dashboard/CLI, Netlify drop, GitHub Pages) confirming no build step or server-side rewrite rules are needed; verified every asset `index.html` references resolves with HTTP 200 from the repo root via a local static-file check.
